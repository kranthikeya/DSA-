def Rev_Str(s):
    s = list(s)
    p = 0
    q = len(s)-1
    while p < q:
        s[p],s[q] = s[q],s[p]
        
        p += 1
        q -=1
    return ''.join(s)
    
s = "ihtnark"
print(Rev_Str(s))
